class Recording {
  String id, title, artistId, artist, artistImageUrl, recordingUrl;
  int upVotes, playCount;

  Recording(this.id, this.title, this.artistId, this.artist,
      this.artistImageUrl, this.recordingUrl, this.upVotes, this.playCount);
}
